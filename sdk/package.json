{
  "name": "notchapp-widget-workspace",
  "private": true,
  "scripts": {
    "test": "node --test tests/**/*.test.mjs"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "chokidar": "^4.0.3",
    "esbuild": "^0.25.10"
  }
}
